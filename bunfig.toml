[serve.static]
plugins = ["bun-plugin-svelte"]

[test]
# Coverage is enabled per-run via `bun test --coverage` (see the test:coverage
# script) so the default `bun test` stays fast.
coverageReporter = ["text", "lcov"]
coverageThreshold = { line = 0.9, function = 0.9 }
coverageSkipTestFiles = true
